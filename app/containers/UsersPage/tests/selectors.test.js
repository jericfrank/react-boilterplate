// import { fromJS } from 'immutable';
// import { makeSelectUsersPageDomain } from '../selectors';

// const selector = makeSelectUsersPageDomain();

describe('makeSelectUsersPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
